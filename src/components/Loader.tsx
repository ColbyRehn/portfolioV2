import './loader.scss';

const Loader = () => {
    return (
        <div> 
            <h2 aria-label="Loading">
                Loading...
            </h2>
        </div>
    );
}

export default Loader;
