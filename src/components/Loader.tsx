import './loader.scss';

const Loader:React.FC = () => {
    return (
        <div> 
            <h2 aria-label="Loading">
                Loading...
            </h2>
        </div>
    );
}

export default Loader;
